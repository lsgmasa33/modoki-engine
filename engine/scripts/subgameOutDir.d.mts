/** Type sidecar for subgameOutDir.mjs — see engine/tests/architecture/mjsTypeSidecars.test.ts.
 *  The export SET here is guarded against the implementation; keep them in step. */

export const SUBGAME_DIST_DIRNAME: string;
export function subgameOutDir(projectRoot: string): string;
